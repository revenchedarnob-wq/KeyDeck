# Proof 0.27 — Canonical Project Brain + Context Inspection Contract (Reconstructed)

Acceptance contract:

- durable KeyDeck-owned Project Brain revision stream;
- revision binds exact verified context packet and source fingerprint;
- included and omitted inspection evidence is preserved;
- restart preserves the revision chain;
- stale source, tampered packet and secret-like content are rejected before new canonical revision persistence;
- Proof Receipt binds the exact Project Brain/context inspection evidence.

Run: `go run ./cmd/proof27`

Status in this reconstructed line: PASS 10/10.
